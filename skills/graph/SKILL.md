---
name: graph
description: Use IWE CLI to find, retrieve, analyze, create, update, or safely refactor an IWE Markdown knowledge graph.
compatibility: Requires IWE CLI >=0.20.0.
allowed-tools: Bash(iwe:*)
---
!`iwe docs agent`

Without injection, run `iwe docs agent` and follow it; an unknown command means iwe is older than 0.20.0 — say so and stop.
