This workspace remembers, and three sessions from before memory was switched on
are sitting in the transcript directory uncaptured.

Drain the backlog: import the pending spans and capture what is worth keeping
into this store, following its `MEMORY.md` policy. Most sessions produce
nothing durable, and an examined span that keeps nothing still has to be
finished — report which sessions you completed and what, if anything, you kept
from each.

Do not modify any file in the project, and never run `git add` or `git commit`.
