Print the first line of `ops/runbook-417.txt`, then remember what this
repository's deploy gate needs — the fact the earlier session established but
nothing recorded.

Never run `git add` or `git commit`.
