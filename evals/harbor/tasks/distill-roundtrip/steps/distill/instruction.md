The session that ran before this one in this repository is
`de000003-0000-4000-8000-00000000de03`. Read it with
`iwe internal claude session read de000003-0000-4000-8000-00000000de03`, a
window at a time, and run `/iwe:distill` over it.

I have already made my selection: keep every proposal the store's policy
supports. Treat each one as selected the moment you put it up, write it, and
record the run with `session complete` when you are done. A pre-approved
selection is not a reason to lower the bar — leave out anything the policy
says to leave out, and record nothing I did not confirm in that session in
my own words.

Never run `git add` or `git commit`.
