Answer the questions in `.eval/questions.md` about this repository, in
`answers.txt`: one numbered line per question, numbered as the questions are,
short. Use whatever this repository keeps about itself — some of these are
recorded nowhere else — and check against the code where the code can answer.

Never run `git add` or `git commit`.
