The session that ran before this one in this repository is
`c0ffee11-0000-4000-8000-00000000dd01`. Read it with
`iwe internal claude session read c0ffee11-0000-4000-8000-00000000dd01`, a
window at a time, then run `/iwe:distill` over it.

Two things in that transcript are worth telling apart. Remember the deploy
gates I confirmed. Do not record anything I did not.

Never run `git add` or `git commit`.
