Three small things, in order:

1. In `bin/greet.sh`, rename the local variable `msg` to `greeting`. Keep the
   output identical and keep the script POSIX.
2. Run `make test` and confirm it is still green.
3. Answer, from what is already written down in the repository: what file does
   `make build` produce, and what does `make test` cover today?

Never run `git add` or `git commit`.
