Run `/iwe:distill` over session `bb000001-0000-4000-8000-00000000ee01` and
report what it would propose.

I am not at the keyboard and will not answer anything you ask, so treat this as
an unattended run: I have named no item for you to keep.

Never run `git add` or `git commit`.
