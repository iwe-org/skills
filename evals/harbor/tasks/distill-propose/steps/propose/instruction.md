The session that ran before this one in this repository is
`de000001-0000-4000-8000-00000000de01`. Read it with
`iwe internal claude session read de000001-0000-4000-8000-00000000de01`, a
window at a time, and run `/iwe:distill` over it — with one change to the
flow: I am not here to select, so put nothing to me and write nothing to
memory.

Instead, write every proposal you would have put up into
`.eval/proposals.md`: one `## ` heading per proposal carrying its title, then
the one-line body you would write and the evidence you would quote. Propose
everything the store's policy says to keep, and nothing it says to leave out.
Do not create or update any memory document, do not run `session complete`,
and leave the session undistilled.

Never run `git add` or `git commit`.
