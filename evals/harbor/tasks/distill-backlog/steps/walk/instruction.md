This repository has sessions on disk that memory has never read.

List them, tell me what is waiting, and then work the backlog: remember what
the deploy session established. Leave anything that is still someone else's
live conversation alone, and mark the rest as seen rather than reading it.

Never run `git add` or `git commit`.
